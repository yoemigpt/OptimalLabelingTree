Optimal Tree Labeling

First run the command `make`

To test with one file, `make run FILE=file_path`

To test with set of files, put all files in the one folder `folder` and `make run-all FOLDER=folder_path`
